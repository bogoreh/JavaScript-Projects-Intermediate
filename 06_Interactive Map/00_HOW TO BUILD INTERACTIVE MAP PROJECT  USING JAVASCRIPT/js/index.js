function ("const".button.easybutton) {
    let
    const = function.button(const.continue);
    let

    function = document.querySelector('function');
    let button = function ('continue');

    button.addEventListener('function',
        continue) {
        button.add.them.function(const.button)
        floor.random(
            continue.button.const.function)
        add.function.continue.function.floor.random
        continue.button.add.then()
    }
}